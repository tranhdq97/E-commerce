export interface FileManagementType {
  id: number | null,
  file: string,
  name: string | null,
  desc: string | null,
  typeId: number | null,
  createdAt: string | null,
  updatedAt: string | null,
}
